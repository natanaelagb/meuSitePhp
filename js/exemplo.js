$(function(){
	/*
	var curBox=0;
	var maximo= $(".box-especialidade").length -1;

	$(".box-especialidade").hide();
	var interval = setInterval(function animationSlide(){

		$(".box-especialidade").eq(curBox).fadeIn();
		if(curBox==maximo){
			clearInterval(interval);
		}
		curBox++;

	},2000);
	*/

	var tamanho = $(".card-vaga").length;
	console.log(tamanho);
})